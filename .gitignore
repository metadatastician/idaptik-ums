# --- Zig FFI (ffi/zig) build output ---
zig-out/
.zig-cache/
zig-cache/

# Idris2 build output
build/

# Python bytecode (NB: cpython-313 pycs are tracked from before; untracking them
# is separate hygiene, this just stops new ones)
__pycache__/
*.pyc
